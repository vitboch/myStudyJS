const results = [3, 5, 2, 1, 2, 3, 4, 6, 1, 5];

function searchesLastIndexElement(element) {
    return results.lastIndexOf(element);
}

let element = 5;
console.log(searchesLastIndexElement(element));